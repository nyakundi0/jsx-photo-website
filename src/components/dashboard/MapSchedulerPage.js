import React from 'react';
import PageWrapper from './PageWrapper'; // Ensure the correct import path

const MapSchedulerPage = () => (
    <PageWrapper title="Map Scheduler">
        <p>Map Scheduler Page content goes here.</p>
    </PageWrapper>
);

export default MapSchedulerPage;